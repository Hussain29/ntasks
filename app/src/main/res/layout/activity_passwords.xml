<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".rents.PasswordsActivity.java">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingLeft="@dimen/_20sdp"
        android:paddingRight="@dimen/_20sdp"
        android:paddingTop="@dimen/_30sdp"
        android:paddingBottom="@dimen/_30sdp">

        <Spinner
            android:id="@+id/passwordSpinner"
            android:layout_width="310dp"
            android:layout_height="45dp"
            android:layout_gravity="center"
            android:layout_marginBottom="16dp"
            style="@style/Widget.AppCompat.Spinner.DropDown"
            android:background="@drawable/spinnerbg" />

        <EditText
            android:id="@+id/currentPasswordEditText"
            style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/passwordSpinner"
            android:layout_marginTop="16dp"
            android:hint="Enter Current Password" />

        <EditText
            android:id="@+id/newPasswordEditText"
            style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/currentPasswordEditText"
            android:layout_marginTop="16dp"
            android:hint="Enter New Password" />

        <EditText
            android:id="@+id/reEnterNewPasswordEditText"
            style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/newPasswordEditText"
            android:layout_marginTop="16dp"
            android:hint="Re-Enter New Password" />

        <Button
            android:id="@+id/changePasswordButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/reEnterNewPasswordEditText"
            android:layout_marginTop="24dp"
            android:text="Change Password" />

    </LinearLayout>

</ScrollView>
